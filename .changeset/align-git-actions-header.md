---
"helmor": patch
---

Fix a visual alignment issue in the Git Actions header:
- The colored Actions button (shown in fix, resolve conflicts, merge, merged, and closed modes) now sits flush with the PR number button next to it, instead of appearing 1–2px shorter.
