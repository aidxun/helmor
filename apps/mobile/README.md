# Helmor Mobile

Expo SDK 56 development-build skeleton for the Helmor mobile shell.

```bash
bun run start
bun run ios
```

`start` expects a development client. Use `bun run ios` to generate and install one locally when Xcode is available.
